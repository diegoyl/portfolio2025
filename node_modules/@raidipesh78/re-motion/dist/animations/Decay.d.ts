import { FluidAnimation, EndResultType } from './FluidAnimation';
export declare type DecayConfig = {
    velocity?: number;
    deceleration?: number;
    delay?: number;
};
export declare class Decay extends FluidAnimation {
    private startTime;
    private fromValue;
    private position;
    private lastPosition;
    private velocity;
    private deceleration;
    private delay;
    private timeout;
    private animationFrame;
    private onFrame;
    constructor(config: DecayConfig);
    start(value: number, onFrame: (value: number) => void, onEnd: (result: EndResultType) => void | null, previousAnimation: FluidAnimation | null): void;
    stop(): void;
    onUpdate(): void;
}
