import { FluidAnimation, EndResultType } from './FluidAnimation';
export declare type TimingConfig = {
    toValue: number;
    duration?: number;
    easing?: (value: number) => number;
    delay?: number;
};
export declare class Timing extends FluidAnimation {
    private position;
    private startTime;
    private fromValue;
    private toValue;
    private duration;
    private delay;
    private timeout;
    private animationFrame;
    private easing;
    private onFrame;
    constructor(config: TimingConfig);
    start(value: number, onFrame: (value: number) => void, onEnd: (result: EndResultType) => void | null, previousAnimation: FluidAnimation | null): void;
    stop(): void;
    onUpdate(): void;
}
