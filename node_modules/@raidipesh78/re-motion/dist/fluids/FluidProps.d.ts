import { Fluid } from './Fluid';
export declare class FluidProps extends Fluid {
    private props;
    private callback;
    constructor(props: Record<string, any>, callback: () => void);
    attach(): void;
    detach(): void;
    get(): Record<string, any>;
    update(): void;
}
