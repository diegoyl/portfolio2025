import type { ControllerAnimation } from './types';
declare type ParallelConfig = {
    stopTogether?: boolean;
};
export declare const parallel: (animations: ControllerAnimation[], config?: ParallelConfig) => ControllerAnimation;
export {};
