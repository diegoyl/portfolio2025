import { FluidValue } from '../fluids/FluidValue';
import type { ControllerAnimation } from './types';
declare type NativeConfig = {
    toValue: number | string;
    delay?: number;
};
export declare const native: (value: FluidValue, config: NativeConfig) => ControllerAnimation;
export {};
